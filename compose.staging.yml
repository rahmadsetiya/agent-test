services:
  app:
    build:
      context: .
    command: ["sleep", "infinity"]
    healthcheck:
      test: ["CMD", "python", "hello.py", "Healthcheck"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    restart: unless-stopped
